apiVersion: v1
kind: Service
metadata:
  name: {{ include "todoevolve-frontend.fullname" . }}
  labels:
    {{- include "todoevolve-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todoevolve-frontend.selectorLabels" . | nindent 4 }}
