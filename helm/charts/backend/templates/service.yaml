apiVersion: v1
kind: Service
metadata:
  name: {{ include "todoevolve-backend.fullname" . }}
  labels:
    {{- include "todoevolve-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todoevolve-backend.selectorLabels" . | nindent 4 }}
