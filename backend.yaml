apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-backend
  template:
    metadata:
      labels:
        app: todo-backend
    spec:
      containers:
      - name: backend
        image: 
        ports:
        - containerPort: 8000
        env:
        - name: FRONTEND_URL
          value: ""
        - name: CORS_ORIGINS
          value: "*"
        - name: GEMINI_API_KEY
          value: ""
        - name: SMTP_USER
          value: ""
        - name: SMTP_PASS
          value: ""
---
apiVersion: v1
kind: Service
metadata:
  name: todo-backend-service
spec:
  type: ClusterIP
  selector:
    app: todo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
