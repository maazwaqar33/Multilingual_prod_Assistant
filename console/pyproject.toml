[project]
name = "todoevolve-console"
version = "0.1.0"
description = "TodoEvolve Console App - Phase I of Smart Multilingual Productivity Assistant"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
